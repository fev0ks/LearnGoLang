# Протоколы и паттерны взаимодействия

## Содержание

- [Что здесь лежит и почему «протоколы» — не совсем точное название](#что-здесь-лежит-и-почему-протоколы--не-совсем-точное-название)
- [Материалы](#материалы)
- [Как читать](#как-читать)
- [Границы с соседними разделами](#границы-с-соседними-разделами)
- [Вопросы для самопроверки](#вопросы-для-самопроверки)

Каталог отвечает на вопрос «как два процесса договариваются об обмене данными и что при этом ломается». Внутри — и собственно сетевые протоколы, и стили поверх них, и форматы описания контракта, и прикладные паттерны интеграции.

---

## Что здесь лежит и почему «протоколы» — не совсем точное название

Название каталога историческое и шире своего буквального смысла. Материалы делятся на четыре разных класса сущностей, и путать их не стоит — на собеседовании вопрос «REST это протокол?» задают именно с этим прицелом.

| Класс | Что это | Файлы |
| --- | --- | --- |
| Протокол | правила обмена на проводе: формат кадров, состояния соединения, коды | TCP, HTTP/1.1, HTTP/2, HTTP/3, WebSocket, WebRTC, SOAP |
| Архитектурный стиль | набор ограничений поверх существующего протокола, а не свой формат | REST, GraphQL (у него есть и свой язык запросов), gRPC как соглашение поверх HTTP/2 |
| Формат описания контракта | язык, на котором API описывают, чтобы генерировать код и проверять совместимость | OpenAPI; Protobuf — в [03-go-libraries-and-ecosystem/grpc](../../03-go-libraries-and-ecosystem/grpc/01-protobuf-and-codegen.md) и [api-design](../api-design/10-protobuf-repo-layout.md) |
| Прикладной паттерн | схема взаимодействия, реализуемая любым транспортом | Webhooks, идемпотентность, ограничение частоты запросов |

Быстрая проверка при сомнении: если у сущности есть формат байтов на проводе и правила установления соединения — это протокол. Если она описывает, как пользоваться чужим протоколом, — стиль или паттерн.

Подпапки каталога разложены по другой оси — по уровню стека, а не по классу. Поэтому в `01-transport` и `02-http` лежат протоколы, в `03-api-styles` — стили и форматы описания, а `04-realtime` собран по задаче и смешивает классы: WebSocket и WebRTC — протоколы, SSE — формат тела ответа. При раскладке по классам близкие по задаче материалы разъехались бы по разным папкам.

Пограничные случаи, о которых спорят чаще всего:

- **REST** — стиль поверх HTTP: своего формата не вводит, вводит ограничения (отсутствие состояния, кэшируемость, единообразный интерфейс). Разбор — [03-api-styles/01-rest-and-http-semantics.md](./03-api-styles/01-rest-and-http-semantics.md).
- **gRPC** — не отдельный транспортный протокол, а строгое соглашение поверх HTTP/2: один вызов равен одному потоку, статус едет в трейлерах. Отсюда и его ограничения, включая невозможность работать из браузера напрямую.
- **GraphQL** — язык запросов и схема плюс соглашение о том, как это возить по HTTP; собственного транспорта у него нет.
- **SSE** — не протокол, а формат тела ответа `text/event-stream` внутри обычного HTTP-ответа, который сервер не закрывает.
- **Webhooks** — вообще не технология, а паттерн «обратный вызов на HTTP-эндпоинт получателя» со всеми вытекающими вопросами доставки и подписи.

---

## Материалы

Подпапки пронумерованы по уровню стека: чем меньше номер, тем ниже уровень и тем меньше в материале зависимостей от остального.

**Точка входа**

- [00. Сравнение протоколов API](./00-protocol-comparison.md) — большая таблица по всем вариантам, decision tree, смешанные архитектуры (Gateway, BFF)

**[01. Транспорт](./01-transport/README.md)** — то, поверх чего работает всё остальное

- [TCP: надёжность, окна и перегрузка](./01-transport/01-tcp-mechanics.md) — подтверждения и повторные передачи, окно получателя против окна перегрузки, произведение полосы на задержку, MSS и MTU, блокировка головой очереди
- [UDP: датаграммы, границы сообщений и своя надёжность](./01-transport/02-udp.md) — границы сообщений и фрагментация, что приходится дописывать самому, зачем UDP понадобился QUIC, сокет в Go и тихие отказы
- [TLS: шифрование, доверие и стоимость рукопожатия](./01-transport/03-tls.md) — что согласуется в рукопожатии, forward secrecy, цепочка сертификатов, записи, возобновление сессии, чтение ошибок

**[02. HTTP](./02-http/README.md)** — сам протокол и его реализация в Go

- [Версии HTTP: 1.1, 2 и 3](./02-http/01-http-versions.md) — head-of-line blocking на разных уровнях, кадры и потоки, HPACK, h2c и ALPN, QUIC
- [HTTP-сервер в Go](./02-http/02-server-in-go.md) — `net/http`, цепочка middleware, таймауты, graceful shutdown
- [HTTP-клиент в Go](./02-http/03-client-in-go.md) — `Transport`, пул соединений, таймауты, повторные попытки, circuit breaker

**[03. Стили API и контракты](./03-api-styles/README.md)** — как договариваются о смысле запросов

- [REST и семантика HTTP](./03-api-styles/01-rest-and-http-semantics.md) — ограничения Филдинга, уровни зрелости, карта кодов ответов, условные запросы, редиректы, CORS
- [gRPC](./03-api-styles/02-grpc.md) — анатомия вызова на проводе, дедлайны и метаданные, коды ошибок, четыре типа вызовов, interceptors, health check, reflection
- [GraphQL](./03-api-styles/03-graphql.md) — схема, запросы и подписки, проблема N+1 и DataLoader, gqlgen, интроспекция
- [OpenAPI и Swagger](./03-api-styles/04-openapi-and-swagger.md) — спецификация, spec-first против code-first, проверка совместимости, contract testing
- [SOAP](./03-api-styles/05-soap.md) — WSDL, конверт, Fault, вызов из Go, почему проиграл

**[04. Реалтайм](./04-realtime/README.md)** — соединение живёт дольше одного запроса

- [WebSocket](./04-realtime/01-websocket.md) — рукопожатие Upgrade, кадры, паттерн read/write-горутин, Hub, backplane
- [SSE и реалтайм-протоколы](./04-realtime/02-sse.md) — long polling, формат событий, `Last-Event-ID`, прокси и буферизация, стриминг ответов модели
- [WebRTC](./04-realtime/03-webrtc.md) — сигнализация, ICE/STUN/TURN, SDP, Pion, P2P против SFU

**[05. Интеграционные паттерны](./05-integration-patterns/README.md)** — не зависят от выбранного транспорта

- [Webhooks](./05-integration-patterns/01-webhooks.md) — доставка at-least-once, подпись HMAC, ключ идемпотентности, outbox
- [Идемпотентность запросов](./05-integration-patterns/02-idempotency.md) — заголовок `Idempotency-Key`, Redis `SET NX`, PostgreSQL `ON CONFLICT`, конкурентные повторы
- [Ограничение частоты запросов](./05-integration-patterns/03-rate-limiting.md) — fixed window, sliding window, token bucket, реализация в Redis без гонок
- [Рабочие реализации ограничителей](./05-integration-patterns/examples/README.md) — компилируемый Go: fixed window, sliding window, token bucket, тесты

---

## Как читать

1. Начать с [00. Сравнение протоколов API](./00-protocol-comparison.md): decision tree покажет, какой материал вообще нужен под задачу.
2. Прочитать [TCP](./01-transport/01-tcp-mechanics.md), [TLS](./01-transport/03-tls.md) и [версии HTTP](./02-http/01-http-versions.md) раньше остальных: на этих трёх уровнях стоит всё прикладное, и половина его странностей объясняется свойствами нижних.
3. Дальше — по потребности, а не подряд. Типичные маршруты:
   - публичный API наружу: REST → OpenAPI → ограничение частоты → идемпотентность;
   - внутренние сервисы: gRPC → версии HTTP → HTTP-клиент;
   - интерфейс с живыми обновлениями: WebSocket и SSE, затем backplane в [брокерах](../../07-message-brokers-and-streaming/00-comparison.md);
   - интеграция со сторонней системой: webhooks → идемпотентность.
4. Вернуться к [00](./00-protocol-comparison.md) после чтения деталей: таблица и trade-offs читаются иначе, когда механика каждого варианта уже знакома.

---

## Границы с соседними разделами

Чтобы не искать не там:

- **[api-design](../api-design/README.md)** — как проектировать свой API поверх этих протоколов: адреса, ресурсы, тело ошибки, пагинация, версионирование, структура proto-репозитория. Здесь — что механизм означает, там — как им пользоваться в своём контракте.
- **[request-lifecycle](../request-lifecycle/README.md)** — путь одного запроса целиком, от ввода в браузере до отрисовки: DNS, TCP, TLS, край сети, приложение, ответ. Здесь — отдельные механизмы, там — их последовательность и вклад в общее время.
- **[07-message-brokers-and-streaming](../../07-message-brokers-and-streaming/00-comparison.md)** — асинхронный обмен через брокер. Граница простая: здесь клиент знает получателя и ждёт ответа, там сообщение уходит в брокер и живёт своей жизнью.
- **[03-go-libraries-and-ecosystem/grpc](../../03-go-libraries-and-ecosystem/grpc/README.md)** — библиотеки и инструменты вокруг gRPC в Go: protobuf и кодогенерация (формат на проводе, `buf`), `grpc-go`, `connect-go`, `grpc-gateway`. Здесь — протокол и его семантика, там — чем это писать.
- **[reliability-patterns](../../05-system-design/reliability-patterns/README.md)** — таймауты, повторы, circuit breaker, backpressure как общие приёмы устойчивости, независимые от протокола.

---

## Вопросы для самопроверки

- Почему REST не протокол и что тогда протокол в связке «REST поверх HTTP»?
- Что именно gRPC берёт от HTTP/2 и почему из-за этого не работает из браузера напрямую?
- В каком случае SSE достаточно и чем он проще WebSocket с точки зрения инфраструктуры?
- Чем отличается head-of-line blocking на уровне HTTP от такого же на уровне TCP и что с этим сделал QUIC?
- Кто обнаруживает потерю пакета в TCP и почему получатель ничего не запрашивает заново?
- Чем окно получателя отличается от окна перегрузки и почему второе не настраивается из приложения?
- Какой код ответа вернуть, если запрос корректен синтаксически, но противоречит состоянию ресурса?
- Почему `POST` не повторяют автоматически и что нужно, чтобы его стало можно повторять?
- Что произойдёт с долгоживущим соединением за балансировщиком уровня L4 при добавлении новой реплики?
