# Перебор с возвратом

## Содержание

- [Идея](#идея)
- [Универсальный шаблон](#универсальный-шаблон)
- [Все перестановки](#задача-1-все-перестановки)
- [Все подмножества](#задача-2-все-подмножества)
- [Сумма с повторным использованием](#задача-3-сумма-с-повторным-использованием)
- [Поиск слова в сетке](#задача-4-поиск-слова-в-сетке)
- [Расстановка ферзей](#задача-5-расстановка-ферзей)
- [Отсечение](#отсечение-ключевые-приёмы)
- [Сводная таблица сложности](#сводная-таблица-сложности)
- [Типичные ошибки](#типичные-ошибки)
- [Interview-ready answer](#interview-ready-answer)

Перебор с возвратом (backtracking) обходит дерево вариантов, отсекает бесперспективные ветви и восстанавливает состояние после каждой ветви. Центральный инвариант — соседние ветви должны начинаться из одного и того же состояния.

---

## Перебор с возвратом

### Идея

Backtracking — это рекурсивный обход **дерева состояний** (state space tree). На каждом узле дерева
мы делаем выбор из доступных вариантов, уходим вглубь, а после возврата из рекурсии отменяем выбор
(undo). Таким образом каждая ветка дерева соответствует одному пути от корня до листа.

**Отсечение (`pruning`)** обрывает ветвь, которая уже не может привести к
ответу. Оно может радикально уменьшить реальное дерево поиска, но обычно не
меняет экспоненциальную оценку худшего случая.

### Универсальный шаблон

```go
func backtrack(path []int, choices []int, result *[][]int) {
    if /* base case — путь полный */ {
        tmp := make([]int, len(path))
        copy(tmp, path)
        *result = append(*result, tmp)
        return
    }
    for _, choice := range choices {
        // 1. сделать выбор
        path = append(path, choice)
        // 2. уйти глубже с уменьшенным пространством выборов
        backtrack(path, /* reduced choices */, result)
        // 3. отменить выбор (undo)
        path = path[:len(path)-1]
    }
}
```

Три шага — сделать выбор, уйти в рекурсию и отменить выбор — повторяются в
каждой задаче на backtracking.
`path` — текущий путь от корня до текущего узла. `result` передаётся по указателю,
потому что `append` должен менять заголовок одного результирующего среза во всех
уровнях рекурсии; уже сохранённые решения всё равно копируются отдельно.

Важно: при сохранении `path` в результат нужно делать `copy`, иначе сохранённые
срезы могут разделять базовый массив и изменяться при последующем откате.

### Задача 1: все перестановки

Дан массив уникальных целых чисел. Вернуть все возможные перестановки.

```go
func permute(nums []int) [][]int {
    result := [][]int{}
    used := make([]bool, len(nums))

    var backtrack func(path []int)
    backtrack = func(path []int) {
        if len(path) == len(nums) {
            tmp := make([]int, len(path))
            copy(tmp, path)
            result = append(result, tmp)
            return
        }
        for i, num := range nums {
            if used[i] {
                continue
            }
            used[i] = true
            path = append(path, num)
            backtrack(path)
            path = path[:len(path)-1]
            used[i] = false
        }
    }

    backtrack([]int{})
    return result
}
```

Слайс `used` длиной N отслеживает, какой элемент уже стоит в текущем `path`. На каждом уровне
рекурсии перебираем все N элементов, пропуская уже использованные — это и формирует дерево
перестановок. Отмена выбора включает как усечение `path`, так и сброс `used[i] = false`.

**Сложность:** O(N! · N) по времени и O(N) вспомогательной памяти на стек,
`path` и `used`. Сам результат занимает Θ(N! · N), и его нельзя исключить при
оценке фактического потребления процесса.

### Задача 2: все подмножества

Дан массив уникальных целых чисел. Вернуть все подмножества (включая пустое и само множество).

```go
func subsets(nums []int) [][]int {
    result := [][]int{}

    var backtrack func(start int, path []int)
    backtrack = func(start int, path []int) {
        // добавляем текущий path на каждом уровне, не только на листьях
        tmp := make([]int, len(path))
        copy(tmp, path)
        result = append(result, tmp)

        for i := start; i < len(nums); i++ {
            path = append(path, nums[i])
            backtrack(i+1, path)
            path = path[:len(path)-1]
        }
    }

    backtrack(0, []int{})
    return result
}
```

Параметр `start` гарантирует, что мы берём только элементы правее текущего — это исключает дубликаты
подмножеств. В отличие от перестановок, результат пишется не только на листьях, а на каждом узле
дерева, потому что каждый префикс пути является допустимым подмножеством.

**Сложность:** O(2^N · N) по времени, O(N) вспомогательной памяти и
Θ(2^N · N) памяти под возвращаемый результат.

### Задача 3: сумма с повторным использованием

Дан массив уникальных натуральных чисел `candidates` и целевая сумма `target`. Найти все комбинации,
чьи элементы в сумме дают `target`. Один и тот же элемент можно использовать несколько раз.

```go
func combinationSum(candidates []int, target int) [][]int {
    // Не меняем порядок во входном срезе вызывающего кода.
    candidates = append([]int(nil), candidates...)
    sort.Ints(candidates) // сортировка для раннего выхода
    result := [][]int{}

    var backtrack func(start, remaining int, path []int)
    backtrack = func(start, remaining int, path []int) {
        if remaining == 0 {
            tmp := make([]int, len(path))
            copy(tmp, path)
            result = append(result, tmp)
            return
        }
        for i := start; i < len(candidates); i++ {
            if candidates[i] > remaining {
                break // pruning: отсортированный массив — дальше только больше
            }
            path = append(path, candidates[i])
            backtrack(i, remaining-candidates[i], path) // i, не i+1: элемент можно повторять
            path = path[:len(path)-1]
        }
    }

    backtrack(0, target, []int{})
    return result
}
```

Два ключевых отличия от `subsets`: рекурсия уходит с тем же `i` (не `i+1`), позволяя повторное
использование элемента; `break` вместо `continue` — после сортировки все последующие кандидаты
тоже превысят `remaining`, продолжать бессмысленно.

**Сложность:** удобнее выражать как O(число посещённых узлов + общий размер
ответа). Грубая верхняя граница — O(N^(T/M)), где `T` — target, а `M` —
минимальный кандидат: глубина не больше `T/M`, ветвление не больше `N`.
Граница намеренно неточная, а худший случай остаётся экспоненциальным. Она
требует заявленного в условии предусловия: кандидаты уникальны и строго
положительны; нулевой кандидат вызвал бы бесконечную рекурсию.

### Задача 4: поиск слова в сетке

Дана 2D-сетка символов и строка `word`. Определить, существует ли путь в сетке, спеллующий `word`.
Из каждой клетки можно двигаться в 4 стороны, одну клетку нельзя использовать дважды в одном пути.

```go
func exist(board [][]byte, word string) bool {
    if len(word) == 0 {
        return true
    }
    if len(board) == 0 {
        return false
    }

    rows := len(board)
    dirs := [][2]int{{0, 1}, {0, -1}, {1, 0}, {-1, 0}}

    var dfs func(r, c, idx int) bool
    dfs = func(r, c, idx int) bool {
        if idx == len(word) {
            return true
        }
        if r < 0 || r >= rows || c < 0 || c >= len(board[r]) {
            return false
        }
        if board[r][c] != word[idx] {
            return false
        }

        // пометить клетку как посещённую — in-place modification
        tmp := board[r][c]
        board[r][c] = '#'

        for _, d := range dirs {
            if dfs(r+d[0], c+d[1], idx+1) {
                board[r][c] = tmp // восстановить перед возвратом
                return true
            }
        }

        // undo: восстановить клетку
        board[r][c] = tmp
        return false
    }

    for r := 0; r < rows; r++ {
        for c := 0; c < len(board[r]); c++ {
            if dfs(r, c, 0) {
                return true
            }
        }
    }
    return false
}
```

Посещённость отмечается прямо в `board` символом `'#'` — это экономит O(M*N) памяти на отдельный
`visited`. Обязательно восстанавливать `board[r][c] = tmp` при возврате — иначе следующие пути
от других стартовых клеток получат испорченную сетку.

Такой in-place приём предполагает, что `'#'` не встречается ни в поле, ни в
искомом слове. Если алфавит не ограничен, нужен отдельный `visited`: для
произвольного `byte` невозможно выбрать гарантированно свободное значение-маркер.

**Сложность:** O(M * N * 4^L), где L — длина слова; в каждой клетке до 4 направлений на L уровней.

### Задача 5: расстановка ферзей

Расставить N ферзей на доске N x N так, чтобы ни один не бил другого. Вернуть количество решений.
Контракт примера — `n >= 1`; защитная ветка возвращает `0` для остальных
значений. В комбинаторике пустую доску иногда считают одним пустым решением,
поэтому поведение для `n == 0` нужно определять явно.

```go
func totalNQueens(n int) int {
    if n <= 0 {
        return 0
    }
    count := 0
    // используем три множества для O(1) проверки атак
    cols := make([]bool, n)
    diag1 := make([]bool, 2*n-1) // главные диагонали: r-c+n-1
    diag2 := make([]bool, 2*n-1) // побочные диагонали: r+c

    var backtrack func(row int)
    backtrack = func(row int) {
        if row == n {
            count++
            return
        }
        for col := 0; col < n; col++ {
            d1 := row - col + n - 1
            d2 := row + col
            if cols[col] || diag1[d1] || diag2[d2] {
                continue // клетка под атакой — pruning
            }
            // разместить ферзя
            cols[col] = true
            diag1[d1] = true
            diag2[d2] = true

            backtrack(row + 1)

            // убрать ферзя
            cols[col] = false
            diag1[d1] = false
            diag2[d2] = false
        }
    }

    backtrack(0)
    return count
}
```

Ферзи расставляются по одному на строку (row), поэтому атаку по строке проверять не нужно.
Три булевых массива кодируют занятость столбца и двух диагоналей за O(1) без итерации по доске.
Формула `r - c + n - 1` отображает главную диагональ (r - c = const) в неотрицательный индекс;
`r + c` — для побочной.

**Сложность:** O(N!) в худшем случае, на практике значительно меньше благодаря pruning. Пространство O(N).

### Отсечение: ключевые приёмы

**1. Сортировка + ранний break**

Если массив отсортирован и нас интересуют суммы, как только текущий элемент превышает остаток —
все последующие тоже превысят. Используем `break`, не `continue`.

```go
sort.Ints(nums)
for i := start; i < len(nums); i++ {
    if nums[i] > remaining {
        break
    }
    // ...
}
```

**2. Пропуск дубликатов при отсортированном входе**

Если входной массив содержит повторяющиеся элементы и нужны уникальные комбинации, пропускаем
элемент, если он совпадает с предыдущим на том же уровне рекурсии:

```go
sort.Ints(nums)
for i := start; i < len(nums); i++ {
    if i > start && nums[i] == nums[i-1] {
        continue // пропускаем дубликат на этом уровне
    }
    // ...
}
```

Условие `i > start` (а не `i > 0`) важно: мы разрешаем использовать одно и то же значение
на разных уровнях рекурсии, запрещаем лишь второй выбор одного значения на том же уровне.

---

## Сводная таблица сложности

В колонке «Память» указана вспомогательная память без возвращаемого результата. Для генераторов перестановок и подмножеств сам результат имеет экспоненциальный размер.

| Задача | Время | Память |
| --- | --- | --- |
| Permutations | O(N! · N) | O(N) |
| Subsets | O(2^N · N) | O(N) |
| Combination Sum | O(N^(T/M)), грубая граница | O(T/M) |
| Word Search | O(M·N·4^L) | O(L) |
| N-Queens | O(N!) | O(N) |

---

## Типичные ошибки

**Перебор с возвратом:**
- Забыть `copy` при сохранении `path` — результаты могут разделять базовый
  массив и перезаписываться следующими ветвями.
- Сохранять `path` без понимания aliasing: `append` может оставить прежний
  базовый массив или выделить новый, поэтому результат нельзя заставлять
  зависеть от конкретного поведения аллокатора.
- Не восстанавливать изменённые данные (например, `board` в Word Search).
- Использовать `continue` вместо `break` при pruning по отсортированному массиву.

---

## Interview-ready answer

**1. Чем перебор с возвратом отличается от полного перебора?**

- Модель — это обход дерева решений с отказом от ветвей, которые уже не могут привести к ответу.
- Нижняя граница — если задача требует вернуть все `2^n` подмножеств или `n!` перестановок, размер результата сам сохраняет экспоненциальную сложность.
- Отсечение — в constraint-задачах вроде N-Queens оно уменьшает дерево поиска, хотя худший случай обычно остаётся экспоненциальным.
- Откат — после рекурсивного вызова состояние восстанавливается, чтобы соседние ветви не влияли друг на друга.

**2. Какая ошибка в Go-реализации перебора встречается чаще всего?**

- Aliasing — сохранённый `path` может разделять базовый массив с рабочим путём и перезаписываться следующими ветвями.
- Копия — при добавлении ответа используют `append([]int(nil), path...)` или `slices.Clone(path)`.
- Откат — все общие структуры, например `used` или клетки поля, восстанавливаются даже при раннем возврате.
- `append` — он может как переиспользовать массив, так и выделить новый; корректность не должна зависеть от оставшейся ёмкости среза.
