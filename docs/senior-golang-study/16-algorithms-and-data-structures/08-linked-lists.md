# Связные списки

## Содержание

- [Ментальная модель и техники](#техники)
- [Развернуть список](#задача-1-развернуть-список)
- [Найти середину](#задача-2-найти-середину)
- [Найти начало цикла](#задача-3-начало-цикла)
- [Слить отсортированные списки](#задача-4-слияние-отсортированных-списков)
- [Удалить N-й узел с конца](#задача-5-удалить-n-й-с-конца)
- [Проверить палиндром](#задача-6-проверка-на-палиндром)
- [Сводная таблица сложности](#сводная-таблица-сложности)
- [Типичные ошибки](#типичные-ошибки)
- [Interview-ready answer](#interview-ready-answer)

Связный список не поддерживает доступ по индексу: алгоритм хранит только несколько ссылок на узлы и должен сохранять следующую ссылку до перезаписи. Основные приёмы — фиктивная голова, разворот связей и два указателя с заданным расстоянием или скоростью.

---

## Связные списки

### Определение узла

```go
type ListNode struct {
    Val  int
    Next *ListNode
}
```

Все задачи ниже используют эту структуру.

### Техники

**Фиктивная голова (`dummy head`)**

Добавление фиктивного узла перед головой списка убирает особые случаи при удалении или вставке
у начала: код для первого узла становится таким же, как для всех остальных.

```go
dummy := &ListNode{Next: head}
curr := dummy
// ... операции ...
return dummy.Next
```

**Два указателя в списке (`two pointers`)**

Два указателя с разными скоростями или смещением решают целый класс задач:
- fast (шаг 2) + slow (шаг 1) → нахождение середины списка
- fast на N шагов вперёд + slow → N-й с конца
- fast + slow одновременно → обнаружение цикла (Floyd's algorithm)

### Задача 1: развернуть список

Дан односвязный список. Вернуть его развёрнутую версию.

```go
// Итеративный вариант — O(N) время, O(1) память
func reverseList(head *ListNode) *ListNode {
    var prev *ListNode
    curr := head
    for curr != nil {
        next := curr.Next // сохранить следующий перед перезаписью
        curr.Next = prev  // развернуть указатель
        prev = curr
        curr = next
    }
    return prev // prev — новая голова
}

// Рекурсивный вариант — O(N) время, O(N) стек
func reverseListRec(head *ListNode) *ListNode {
    if head == nil || head.Next == nil {
        return head
    }
    newHead := reverseListRec(head.Next)
    head.Next.Next = head // узел, стоявший за head, теперь смотрит на head
    head.Next = nil       // head становится хвостом
    return newHead
}
```

Итеративный вариант не расходует по кадру стека на каждый узел. Стек goroutine
в Go растёт динамически, но рекурсия длиной O(N) всё равно удерживает O(N)
памяти и на достаточно большом списке может исчерпать лимит стека или память.
Три переменных `prev / curr / next` — достаточное состояние для одного шага.

**Сложность:** O(N) время, O(1) доп. память (итеративный).

### Задача 2: найти середину

Дан односвязный список. Вернуть узел, являющийся серединой. При чётном числе узлов вернуть
второй из двух средних (LeetCode 876 по умолчанию).

```go
func middleNode(head *ListNode) *ListNode {
    slow, fast := head, head
    for fast != nil && fast.Next != nil {
        slow = slow.Next
        fast = fast.Next.Next
    }
    return slow
}
```

Инвариант: когда `fast` достигает конца (nil или последний узел), `slow` находится ровно
посередине. При нечётном N (например, 5 узлов) fast дойдёт до последнего узла, slow будет
на 3-м. При чётном N (например, 4 узла) fast станет nil, slow будет на 3-м из 4 — второй средний.

**Сложность:** O(N) время, O(1) память.

### Задача 3: начало цикла

Определить, есть ли цикл в списке. Если есть — вернуть узел, с которого цикл начинается.

```go
func detectCycle(head *ListNode) *ListNode {
    slow, fast := head, head

    // Фаза 1: обнаружение цикла
    for fast != nil && fast.Next != nil {
        slow = slow.Next
        fast = fast.Next.Next
        if slow == fast {
            // Фаза 2: поиск входа в цикл
            // Один указатель — с начала списка, второй — с точки встречи,
            // оба движутся по одному шагу; встретятся на входе в цикл.
            entry := head
            for entry != slow {
                entry = entry.Next
                slow = slow.Next
            }
            return entry
        }
    }
    return nil // цикла нет
}
```

Математическое обоснование фазы 2: пусть расстояние от head до входа в цикл — F,
от входа до точки встречи — a, длина цикла — C. В момент встречи slow прошёл F + a,
fast — F + a + k*C (k >= 1 полных оборотов). Так как fast = 2 * slow:
2(F + a) = F + a + k*C → F = k*C - a. Указатель с начала пройдёт F шагов,
slow внутри цикла также пройдёт F = k*C - a шагов и окажется ровно у входа.

**Сложность:** O(N) время, O(1) память.

### Задача 4: слияние отсортированных списков

Даны два отсортированных связных списка. Вернуть слитый отсортированный список.

```go
func mergeTwoLists(list1 *ListNode, list2 *ListNode) *ListNode {
    dummy := &ListNode{}
    curr := dummy

    for list1 != nil && list2 != nil {
        if list1.Val <= list2.Val {
            curr.Next = list1
            list1 = list1.Next
        } else {
            curr.Next = list2
            list2 = list2.Next
        }
        curr = curr.Next
    }

    // Один из списков закончился — присоединить остаток другого
    if list1 != nil {
        curr.Next = list1
    } else {
        curr.Next = list2
    }

    return dummy.Next
}
```

Dummy head убирает необходимость обрабатывать "первый узел результата" отдельно. `curr` всегда
указывает на последний добавленный узел, и мы просто выбираем меньший из текущих голов двух списков.
Финальная проверка обрабатывает хвост непустого списка одним присваиванием.

**Сложность:** O(N + M) время, O(1) доп. память (без учёта выходного списка).

### Задача 5: удалить N-й с конца

Дан связный список и число N. Удалить N-й узел с конца списка и вернуть голову.
Пример следует контракту задачи `1 <= n <= длина списка`; без этой гарантии цикл
сдвига `fast` может разыменовать `nil`, и рабочий API должен вернуть ошибку.

```go
func removeNthFromEnd(head *ListNode, n int) *ListNode {
    dummy := &ListNode{Next: head}
    fast, slow := dummy, dummy

    // fast уходит вперёд на n+1 шагов (не n, потому что slow должен
    // остановиться перед удаляемым узлом, а не на нём)
    for i := 0; i <= n; i++ {
        fast = fast.Next
    }

    // Двигать оба, пока fast не достигнет конца
    for fast != nil {
        fast = fast.Next
        slow = slow.Next
    }

    // slow стоит перед N-м с конца — удалить следующий узел
    slow.Next = slow.Next.Next
    return dummy.Next
}
```

Расстояние между `fast` и `slow` поддерживается равным N+1. Когда `fast` становится nil (прошёл
весь список), `slow` находится ровно перед удаляемым узлом. Dummy head обрабатывает удаление
первого узла без специального кода.

**Сложность:** O(N) время, O(1) память; один проход по списку.

### Задача 6: проверка на палиндром

Определить, является ли связный список палиндромом. Решить за O(N) время и O(1) память.

```go
func isPalindrome(head *ListNode) bool {
    if head == nil || head.Next == nil {
        return true
    }

    // Шаг 1: найти середину
    slow, fast := head, head
    for fast != nil && fast.Next != nil {
        slow = slow.Next
        fast = fast.Next.Next
    }
    // slow теперь указывает на начало второй половины

    // Шаг 2: развернуть вторую половину
    secondHalf := reverseList(slow)
    secondHalfCopy := secondHalf // сохранить для восстановления

    // Шаг 3: сравнить первую и вторую половины
    p1, p2 := head, secondHalf
    result := true
    for p2 != nil {
        if p1.Val != p2.Val {
            result = false
            break
        }
        p1 = p1.Next
        p2 = p2.Next
    }

    // Шаг 4: восстановить список (good practice)
    reverseList(secondHalfCopy)
    return result
}

// reverseList определена в Задаче 1.
```

Алгоритм состоит из четырёх шагов: найти середину (fast/slow), развернуть вторую половину на месте,
сравнить значения узлов обеих половин, восстановить список в исходное состояние. Восстановление
технически необязательно для прохождения теста, но хорошая практика — вызывающий код не должен
получать испорченную структуру данных.

Для нечётного числа узлов, например `1→2→3→2→1`, после поиска середины `slow`
указывает на `3`. Развёрнутый суффикс имеет вид `1→2→3`, поэтому средний узел
сравнивается сам с собой последним. Это не меняет результат. Альтернативный
шаблон ищет конец первой половины и разворачивает `slow.Next`; тогда середина
действительно пропускается.

**Сложность:** O(N) время, O(1) доп. память.

---

## Сводная таблица сложности

| Задача | Время | Вспомогательная память |
| --- | --- | --- |
| Reverse Linked List | O(N) | O(1) |
| Find Middle | O(N) | O(1) |
| Detect Cycle Start | O(N) | O(1) |
| Merge Two Sorted Lists | O(N + M) | O(1) |
| Remove Nth From End | O(N) | O(1) |
| Palindrome Linked List | O(N) | O(1) |

---

## Типичные ошибки

**Связные списки:**
- Обращение к `node.Next` без проверки `node != nil` — паника в рантайме.
- Забыть о фиктивной голове и получить отдельные ветки для изменения первого узла.
- В задаче "Remove Nth from End" сдвигать fast на N вместо N+1 шагов, из-за чего slow останавливается
  на удаляемом узле, а не перед ним.
- Забыть восстановить список после `isPalindrome` — структура остаётся частично развёрнутой.

---

## Interview-ready answer

**1. Зачем нужна фиктивная голова в задачах на связные списки?**

- Назначение — фиктивная голова устраняет отдельный случай вставки или удаления перед первым элементом.
- Инвариант — указатель на предыдущий узел существует даже для исходной головы.
- Результат — функция возвращает `dummy.Next`, а не сам временный узел.
- Цена — создаётся один временный узел; разместит его компилятор на стеке или в куче,
  зависит от escape analysis и не является контрактом языка.

**2. Как найти начало цикла, а не только факт его наличия?**

- Фаза 1 — медленный и быстрый указатели встречаются внутри цикла.
- Фаза 2 — один указатель ставится в голову, после чего оба идут по одному шагу.
- Результат — повторная встреча происходит на входе в цикл, что следует из равенства пройденных расстояний по модулю длины цикла.
- Стоимость — O(n) времени и O(1) памяти против O(n) памяти у множества посещённых узлов.

**3. Как удалить N-й узел с конца за один проход?**

- Разрыв — быстрый указатель уходит от фиктивной головы на `N+1` ссылку, после чего оба идут синхронно.
- Позиция — когда быстрый достигает `nil`, медленный стоит перед удаляемым узлом.
- Причина `N+1` — разрыв в `N` ссылок поставил бы медленный указатель на удаляемый узел, а не перед ним.
- Фиктивная голова — она убирает отдельную ветку для удаления первого узла;
  решить задачу без неё можно, но код получается менее однородным.
