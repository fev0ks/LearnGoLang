# Паттерны алгоритмов: навигатор

## Содержание

- [Как распознать паттерн по условию задачи](#как-распознать-паттерн-по-условию-задачи)
- [Обзор каждого паттерна](#обзор-каждого-паттерна)
  - [Два указателя](#два-указателя-two-pointers)
  - [Скользящее окно](#скользящее-окно-sliding-window)
  - [Бинарный поиск](#бинарный-поиск)
  - [Деревья и графы](#деревья-и-графы)
  - [Динамическое программирование](#динамическое-программирование)
  - [Сортировки и куча](#сортировки-и-куча)
  - [Перебор с возвратом](#перебор-с-возвратом-backtracking)
  - [Связные списки](#связные-списки)
  - [Префиксные суммы и разностные массивы](#префиксные-суммы-и-разностные-массивы)
  - [Монотонный стек и дек](#монотонный-стек-и-дек)
  - [Интервалы и sweep line](#интервалы-и-sweep-line)
  - [Взвешенные графы](#взвешенные-графы)
- [Структура решения задачи на интервью](#структура-решения-задачи-на-интервью)
- [Сигналы для оптимизации](#сигналы-для-оптимизации)
- [Interview-ready answer](#interview-ready-answer)

Этот файл — точка входа в раздел. Его цель: по описанию задачи быстро понять,
какой паттерн применить, и перейти к нужному файлу с деталями и примерами.

---

## Как распознать паттерн по условию задачи

Первый шаг при виде задачи — не писать код, а правильно классифицировать её. Таблица ниже даёт быстрый ответ: какие ключевые слова и структуры данных в условии указывают на конкретный паттерн.

| Признаки задачи | Паттерн | Файл |
|---|---|---|
| Отсортированный массив, найти пару/тройку с заданной суммой | Два указателя с концов | [02](./02-two-pointers-and-sliding-window.md) |
| Связный список: обнаружить цикл, найти середину, k-й элемент с конца | Два указателя, быстрый и медленный | [02](./02-two-pointers-and-sliding-window.md) |
| Непрерывный отрезок с монотонным ограничением (не более K различных, все уникальные) | Скользящее окно | [02](./02-two-pointers-and-sliding-window.md) |
| Массив или пространство ответов отсортировано, нужен O(log n) поиск | Бинарный поиск | [03](./03-binary-search.md) |
| Монотонная предикатная функция: "можно ли выполнить задачу при параметре x" | Бинарный поиск по ответу | [03](./03-binary-search.md) |
| Дерево: найти путь, глубину, LCA, проверить симметрию | Поиск в глубину на дереве | [04](./04-trees-and-graphs.md) |
| Дерево: обход по уровням, минимальная глубина, zigzag-обход | Поиск в ширину на дереве | [04](./04-trees-and-graphs.md) |
| Граф без весов: кратчайший путь, достижимость | Поиск в ширину | [04](./04-trees-and-graphs.md) |
| Граф: компоненты связности, обнаружение цикла, топологический обход | Поиск в глубину | [04](./04-trees-and-graphs.md) |
| Задачи с зависимостями, порядок сборки, расписание курсов | Топологическая сортировка | [04](./04-trees-and-graphs.md) |
| Объединение множеств, проверка компонент связности в онлайн-режиме | Система непересекающихся множеств | [04](./04-trees-and-graphs.md) |
| Задача оптимизации, перекрывающиеся подзадачи, оптимальная подструктура | Динамическое программирование | [05](./05-dynamic-programming.md) |
| "Сколько способов", "минимум шагов", "можно ли набрать сумму" | Динамическое программирование | [05](./05-dynamic-programming.md) |
| Нужны все возможные комбинации, перестановки, подмножества | Перебор с возвратом | [07](./07-backtracking.md) |
| Top-K наибольших/наименьших, медиана потока, слияние K отсортированных списков | Куча | [06](./06-sorting-and-heap.md) |
| Разворот связного списка, слияние двух списков, обнаружение цикла | Приёмы со связными списками | [08](./08-linked-lists.md) |
| Много запросов суммы по неизменяемым диапазонам | Префиксная сумма | [09](./09-prefix-sums-and-difference-arrays.md) |
| Много пакетных прибавлений на диапазонах, результат нужен в конце | Разностный массив | [09](./09-prefix-sums-and-difference-arrays.md) |
| Первый больший или меньший элемент, ближайшая граница, площадь гистограммы | Монотонный стек | [10](./10-monotonic-stack-and-deque.md) |
| Максимум или минимум в каждом движущемся окне | Монотонный дек | [10](./10-monotonic-stack-and-deque.md) |
| Пересекающиеся временные диапазоны, максимальная одновременность | Сортировка интервалов или sweep line | [11](./11-intervals-and-sweep-line.md) |
| Взвешенный граф: кратчайший путь | 0-1 BFS, Dijkstra или Bellman-Ford — по весам | [12](./12-weighted-graphs-and-shortest-paths.md) |

---

## Обзор каждого паттерна

### Два указателя (two pointers)

Суть: использовать два индекса, движущихся по массиву или списку, чтобы избежать вложенного цикла.

Типичная сложность: O(n) по времени, O(1) по памяти.

Две основные конфигурации:
- Указатели с двух концов (opposite ends) — сходятся к центру. Применяется на отсортированном массиве: левый сдвигается вправо, если сумма мала, правый влево — если велика.
- Указатели с разными скоростями (fast/slow) — fast делает два шага, slow один. Позволяет обнаружить цикл (встретились), найти середину (fast дошёл до конца) или k-й элемент с конца (разрыв между указателями равен k).

Типичные задачи: Two Sum II (sorted), 3Sum, Linked List Cycle, Middle of Linked List, Remove Nth Node From End.

### Скользящее окно (sliding window)

Суть: поддерживать окно `[left, right]` над массивом или строкой, расширяя его
вправо и сужая слева при нарушении инварианта.

Типичная сложность: O(n) — каждый элемент входит и выходит из окна ровно один раз.

Ключевая идея: вместо пересчёта агрегата с нуля при каждом сдвиге сумма или
частоты обновляются при входе и выходе элемента. Для переменного окна условие
должно быть монотонным относительно движения границ: после нарушения его можно
восстановить, двигая левую границу только вперёд. Например, окно по сумме
неотрицательных чисел обладает этим свойством, а окно по сумме с отрицательными
числами — обычно нет.

```go
left := 0
freq := make(map[byte]int)
result := 0
for right := 0; right < len(s); right++ {
    freq[s[right]]++
    // окно сужается, пока инвариант нарушен
    for freq[s[right]] > 1 {
        freq[s[left]]--
        left++
    }
    if right-left+1 > result {
        result = right - left + 1
    }
}
```

Типичные задачи: Longest Substring Without Repeating Characters, Minimum Window Substring, Maximum Sum Subarray of Size K.

### Бинарный поиск

Суть: каждый шаг отсекает половину пространства поиска, опираясь на монотонное свойство.

Типичная сложность: O(log n).

Ключевая идея: шаблон с инвариантом `[lo, hi)` или `[lo, hi]` — один вариант выбирается и выдерживается во всём коде. Ошибки почти всегда в граничных условиях: `<=` vs `<`, `mid+1` vs `mid`.

```go
lo, hi := 0, len(nums)-1
for lo <= hi {
    mid := lo + (hi-lo)/2
    if nums[mid] == target {
        return mid
    } else if nums[mid] < target {
        lo = mid + 1
    } else {
        hi = mid - 1
    }
}
return -1
```

Binary Search on answer: поиск идёт не в массиве, а в пространстве ответов. Условие `canDo(mid)` монотонно — при x достаточно большом всегда true, при малом — false. Граница двигается, пока не найдётся минимальный (или максимальный) допустимый x.

Типичные задачи: Search in Rotated Sorted Array, Find First and Last Position, Koko Eating Bananas, Capacity To Ship Packages.

### Деревья и графы

Суть: задачи на деревьях и графах в основе своей — задачи на обход. Выбор между DFS и BFS определяет, что удобнее поддерживать в памяти.

Типичная сложность: O(V + E) для графов, O(n) для деревьев.

DFS на дереве — рекурсия естественна. Постпорядковый обход нужен, когда результат узла зависит от детей (например, высота поддерева). Препорядковый — когда нужно передать информацию от корня вниз.

```go
func dfs(node *TreeNode) int {
    if node == nil {
        return 0
    }
    left := dfs(node.Left)
    right := dfs(node.Right)
    return 1 + max(left, right)
}
```

BFS на дереве — итеративный обход с очередью. Удобен, когда нужно работать с уровнями: на каждой итерации внешнего цикла обрабатывается ровно один уровень (размер очереди в начале итерации).

Union-Find: структура для эффективного объединения множеств и проверки принадлежности. Операции `Union` и `Find` с path compression и union by rank работают за O(α(n)) ≈ O(1).

Топологическая сортировка (алгоритм Кана): считается входящая степень каждого узла; узлы с нулевой степенью попадают в очередь, а при их обработке степень соседей уменьшается.

Типичные задачи: Maximum Depth of Binary Tree, Lowest Common Ancestor, Number of Islands, Course Schedule, Redundant Connection.

### Динамическое программирование

Суть: разбить задачу на перекрывающиеся подзадачи, запомнить результаты (memoization или tabulation) и избежать повторных вычислений.

Типичная сложность: от O(n) до O(n * m) в зависимости от количества состояний.

Два шага построения решения:
1. Определить состояние: что именно означает `dp[i]` — например, «минимальное количество монет для суммы i».
2. Записать переход: как `dp[i]` вычисляется через предыдущие состояния.

Типичные подкатегории:
- 1D DP: Fibonacci, Climbing Stairs, House Robber.
- 2D DP (строки): Longest Common Subsequence, Edit Distance.
- Knapsack: можно взять предметы с ограничением по весу.
- Interval DP: задачи на отрезках, палиндромы.

Признак применимости: если рекурсивное решение с перебором даёт правильный ответ, но не укладывается в лимит времени (TLE, Time Limit Exceeded), стоит проверить, пересчитываются ли одни и те же аргументы. Если да, помогает мемоизация.

Типичные задачи: Coin Change, Longest Increasing Subsequence, Word Break, Unique Paths, 0/1 Knapsack.

### Сортировки и куча

Суть: heap (куча) — структура данных, которая за O(log n) поддерживает доступ к минимальному (или максимальному) элементу.

Типичная сложность операций: O(log n) для push/pop, O(1) для peek.

В Go пакет `container/heap` требует реализации интерфейса из пяти методов. Для
max-heap достаточно изменить направление сравнения в `Less`; инвертировать сами
значения не нужно, и это избегает переполнения на минимальном `int`.

Ключевые применения:
- Top-K наибольших элементов: поддерживается min-heap размером K, и элемент больше вершины вытесняет её.
- Top-K наименьших: поддерживается max-heap размером K.
- Медиана потока: два heap — max-heap для левой половины, min-heap для правой. Размеры половин балансируются.
- Слияние K списков: по одному элементу из каждого списка кладётся в min-heap вместе с указателем на источник.

Сортировка для предобработки: если задача требует O(n log n), часто правильнее сначала отсортировать входные данные, а потом применить жадный подход или два указателя.

Типичные задачи: Kth Largest Element, Find Median from Data Stream, Merge K Sorted Lists, Task Scheduler.

### Перебор с возвратом (backtracking)

Суть: систематический перебор с отсечением (pruning) заведомо неверных ветвей.

Типичная сложность: O(n!) для перестановок, O(2^n) для подмножеств — backtracking не улучшает теоретическую сложность, но на практике отсечения резко сокращают количество проверяемых вариантов.

Шаблон: выбрать элемент, добавить в текущий путь, рекурсивно продолжить, отменить выбор (backtrack).

```go
func backtrack(start int, current []int, result *[][]int, nums []int) {
    *result = append(*result, append([]int{}, current...))
    for i := start; i < len(nums); i++ {
        current = append(current, nums[i])
        backtrack(i+1, current, result, nums)
        current = current[:len(current)-1]
    }
}
```

Отсечение (pruning): перед рекурсивным вызовом проверяется, может ли текущая ветвь вообще привести к ответу. Для задач с суммой — если накопленная сумма уже превысила целевую, дальше идти незачем.

Типичные задачи: Subsets, Permutations, Combination Sum, N-Queens, Sudoku Solver.

### Связные списки

Суть: задачи на связных списках требуют аккуратной работы с указателями. Большинство решаются in-place без дополнительной памяти.

Типичная сложность: O(n) по времени, O(1) по памяти.

Ключевые техники:
- Sentinel node (dummy head): упрощает обработку граничных случаев — не нужно отдельно обрабатывать удаление головы или вставку перед ней.
- Reverse in-place: три указателя (prev, curr, next), итеративно.
- Слияние двух отсортированных списков: сравниваются головы, меньшая присоединяется к результату, указатель сдвигается.
- Определение середины: fast/slow указатели.

```go
// reverse linked list
var prev *ListNode
curr := head
for curr != nil {
    next := curr.Next
    curr.Next = prev
    prev = curr
    curr = next
}
return prev
```

Типичные задачи: Reverse Linked List, Merge Two Sorted Lists, Reorder List и
Copy List with Random Pointer. В LRU Cache список отвечает только за порядок
использования; доступ за O(1) дополнительно требует хеш-таблицы.

### Префиксные суммы и разностные массивы

Суть: один раз накопить агрегат по префиксам, чтобы затем отвечать на запрос по
диапазону за O(1). Для полуинтервала `[left, right)` сумма равна
`prefix[right] - prefix[left]`.

Префиксные суммы подходят для неизменяемых данных. Разностный массив решает
обратную offline-задачу: каждое прибавление на диапазоне отмечается только в его
границах, а итог восстанавливается одним проходом. Если запросы и изменения
чередуются online, обычно нужны Fenwick Tree или Segment Tree.

Отдельный паттерн — префиксная сумма плюс хеш-таблица частот. Он считает
подмассивы с заданной суммой за ожидаемое O(n) даже при отрицательных числах,
когда обычное скользящее окно теряет монотонность.

### Монотонный стек и дек

Суть: хранить только кандидатов, которые ещё могут стать ответом. Из стека или
дека удаляются элементы, навсегда проигравшие новому значению.

Монотонный стек отвечает на вопросы о ближайшем большем или меньшем элементе и
границах прямоугольника в гистограмме. Монотонный дек дополнительно удаляет
индексы, вышедшие из скользящего окна, и получает максимум или минимум каждого
окна за O(n).

Линейная сложность амортизированная: каждый индекс добавляется один раз и
удаляется не больше одного раза, хотя одна конкретная итерация может вытолкнуть
много элементов.

### Интервалы и sweep line

Суть: сначала явно выбрать семантику границ, затем отсортировать интервалы или
их события. Для `[start, end)` интервалы, соприкасающиеся в `end == start`, не
пересекаются.

Сортировка по началу позволяет слить пересечения одним проходом. Sweep line
превращает каждый интервал в события `+1` и `-1` и поддерживает активное
состояние. При одинаковой координате порядок событий является частью
корректности: для полуинтервалов окончание обрабатывается раньше начала.

### Взвешенные графы

Суть: алгоритм кратчайшего пути выбирается по допустимым весам. BFS работает,
когда все переходы равнозначны; 0-1 BFS — только для весов `0/1`; Dijkstra —
для любых неотрицательных весов; Bellman-Ford нужен при отрицательных рёбрах.

Для Dijkstra ключевой инвариант состоит в том, что минимальная извлечённая
дистанция уже окончательна. Отрицательное ребро ломает это доказательство. Если
граф является DAG, достаточно топологического порядка и одной релаксации каждого
ребра, даже когда веса отрицательны.

---

## Структура решения задачи на интервью

Следующий пятишаговый фреймворк помогает не паниковать и систематически прийти к решению.

**1. Уточнить условие (`clarify`)**

Вопросы задаются до написания кода:
- Каков диапазон n? (от этого зависит допустимая сложность)
- Какие значения могут быть в массиве? Есть ли отрицательные числа? Дубликаты?
- Что возвращать, если входные данные пустые?
- Отсортирован ли входной массив?

Ответы на эти вопросы часто указывают на паттерн напрямую.

**2. Назвать наивное решение (`brute force`)**

Самое простое решение и его сложность проговариваются вслух — это показывает понимание задачи. Кодировать его не нужно, достаточно описать словами: «два вложенных цикла, O(n²), явно медленно».

**3. Выбрать улучшение (`optimize`)**

Дальше объясняется, почему наивное решение избыточно и какое наблюдение позволяет его улучшить. Опора — таблица распознавания: «массив отсортирован и нужна пара, значит два указателя». Сложность нового решения тоже называется вслух.

**4. Написать код (`code`)**

Код сопровождается комментарием вслух: «инициализирую два указателя с концов, в цикле проверяю сумму, если она меньше целевой — двигаю левый». Так интервьюер видит ход мысли и может подсказать при отклонении.

**5. Проверить граничные случаи (`test`)**

После написания кода проверяются граничные случаи:
- Пустой входной массив или nil
- Один элемент
- Дубликаты или все одинаковые значения
- Минимальное и максимальное значение n
- Уже отсортированный или обратно отсортированный массив (для сортировочных задач)

---

## Сигналы для оптимизации

Когда есть рабочий, но медленный вариант, следующая таблица помогает найти направление улучшения.

| Что видно в наивном решении | Что применить |
|---|---|
| Два вложенных цикла по одному массиву | Два указателя или хеш-таблица для ожидаемого O(1) поиска |
| Линейный поиск в отсортированном массиве | Бинарный поиск |
| Рекурсия вычисляет одни и те же аргументы | Мемоизация (top-down DP) |
| «Лучший из K» или «K наибольших» в потоке | Куча размером K |
| «Все комбинации» и нет явной оптимизации | Перебор с возвратом и отсечениями |
| Пересчёт суммы/агрегата подмассива с нуля | Префиксная сумма или скользящее окно |
| Для каждого элемента линейно ищется первый больший или меньший | Монотонный стек |
| Для каждого окна заново ищется максимум или минимум | Монотонный дек |
| Попарно сравниваются временные диапазоны | Сортировка интервалов или sweep line |
| Повторяющиеся обращения к одному диапазону | Sparse Table или Segment Tree для range queries |
| Граф с взвешенными рёбрами, кратчайший путь | 0-1 BFS, Dijkstra или Bellman-Ford — после проверки весов |

### Подсказка по сложности

Ограничение на `n` в условии — это подсказка о требуемой сложности, а не деталь оформления. Обратная развёртка таблицы: увидев `n <= 20`, разумно сразу рассматривать экспоненциальный перебор, а увидев `n <= 10^6` — сразу отбрасывать всё, что хуже O(n log n).

| Допустимая сложность | Ориентировочный n |
|---|---|
| O(n!) | n <= 10 |
| O(2^n) | n <= 20 |
| O(n^2) | n <= 10^3 |
| O(n log n) | n <= 10^6 |
| O(n) | n <= 10^7 |
| O(log n) | n <= 10^18 |

---

## Interview-ready answer

**1. Как за минуту понять, какой паттерн применить?**

- Первый шаг — смотреть на структуру входных данных и требуемый результат, а не на сюжет задачи.
- Линейные данные — отсортированный массив с поиском пары указывает на два указателя; непрерывный отрезок с монотонным ограничением — на скользящее окно.
- Повторные подзадачи — «сколько способов» или «минимум шагов» указывают на динамическое программирование; «перечислить все комбинации» — на перебор с возвратом.
- Ограничение — `n <= 20` может разрешить экспоненциальный перебор, а `n` порядка миллиона обычно требует O(n log n) или лучше.

**2. Что даёт ограничение на `n` в условии?**

- Роль — ограничение задаёт верхнюю границу приемлемого числа операций.
- Грубые ориентиры — `n <= 10` допускает O(n!), `n <= 20` — O(2^n), `n <= 10³` — O(n²), `n <= 10⁶` — O(n log n), `n <= 10⁷` — O(n).
- Неявный диапазон — при `n <= 10^18` вход обычно задан не массивом из `n` элементов, а числовым диапазоном или функцией; тогда ищут логарифмический алгоритм.
- Ограничение ориентира — константы, язык, лимит времени, память и стоимость одной операции могут сдвинуть границу на порядки.

**3. Как объяснять решение вслух, чтобы это выглядело сильно?**

- Уточнения — диапазон `n`, отрицательные значения, дубликаты и контракт для пустого входа.
- База — наивное решение и его сложность словами, без обязательного написания кода.
- Улучшение — наблюдение, которое отсекает лишнюю работу: «массив отсортирован и нужна пара, значит можно двигать два указателя».
- Проверка — пустой вход, один элемент, одинаковые элементы и границы диапазона.

**4. Какие сигналы говорят, что рабочее решение можно ускорить?**

- Повторный поиск — два вложенных цикла иногда заменяются двумя указателями или хеш-таблицей, если сохраняется нужный инвариант.
- Упорядоченность — линейный поиск по отсортированным данным заменяется бинарным.
- Повторные состояния — к рекурсии, вычисляющей одни и те же аргументы, добавляется мемоизация.
- Повторный агрегат — пересчёт подмассива с нуля заменяется префиксными суммами или скользящим окном.
- Частичный порядок — полная сортировка ради нескольких крайних элементов заменяется кучей размером `K` или алгоритмом выбора.
- Ближайшая граница — повторный поиск первого большего или меньшего элемента заменяется монотонным стеком.
- Веса графа — сначала проверяется их диапазон: он определяет выбор между BFS, 0-1 BFS, Dijkstra и Bellman-Ford.
